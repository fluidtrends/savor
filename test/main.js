var savor = require('..');
var main  = require('../src/main');

savor.add('should accept input', function(done, context) {
  done && done();
}).

add('should accept dd', function(done, context) {
  done && done();
}).

run();
